import React from 'react';
import styles from './TaskManager.module.css';

const TaskManager = () => {
    return (
        <div className={styles.container}>
            <h1>Category Tasks</h1>
            <p>Drag and Drop board will be here</p>
        </div>
    );
};

export default TaskManager;